# TC Sports Cards — Airtable Schema

> Authoritative schema reference for the TC Sports Cards site (Zeeland, MI).
> Base URL: https://airtable.com/appqso1piJgZ6ybFb
> Base ID: `appqso1piJgZ6ybFb`
> Built on the Global Storefront / Restaurant Retail blueprint (CLAUDE.md §27B).

---

## Environment Variables

Set these in Netlify → Site settings → Environment variables:

```
AIRTABLE_API_KEY=pat...                  # Personal Access Token (rotate after handoff)
AIRTABLE_BASE_ID=appqso1piJgZ6ybFb       # This base
JWT_SECRET=<random 32+ char string>      # Used to sign auth tokens
OPENAI_API_KEY=sk-...                    # Counter Helper bot (gpt-3.5-turbo)
OPENAI_MODEL=gpt-3.5-turbo               # Optional override; defaults to gpt-3.5-turbo
```

The `.env.example` file at the repo root documents the same set for local dev.

---

## Table Overview

| # | Table | Purpose |
|---|---|---|
| 1 | Users | Customer accounts + admin flags + loyalty balance |
| 2 | Products | Online-exclusive card inventory (10–15 hand-picked drops) |
| 3 | Orders | Customer orders for pickup or shipping |
| 4 | Ratings | Product reviews (1–5 stars + optional comment) |
| 5 | RewardsTiers | Tier ladder (Rookie / Veteran / All-Star / Hall of Famer) |
| 6 | GiftCardRedemptions | Points-to-gift-card conversions for in-store use |
| 7 | SiteImages | Slot-based image management (hero, banners, about) |
| 8 | SiteConfig | Key/value content config (text, hours, social links) |
| 9 | BotConversations | Counter Helper chat turn log (one row per user→bot exchange) |

User identity key across the site is **Email** (case-insensitive). JWT tokens carry `userId` (Airtable record ID) + `email` + `isAdmin`.

---

## 1. Users (`tblji5mS3IX0SmKvf`)

Customer + admin records. One row per registered account.

| Field | Type | Notes |
|---|---|---|
| Name | singleLineText | Display name |
| Email | email | **Unique login key.** Case-insensitive match in functions. |
| PasswordHash | multilineText | bcryptjs hash (10 salt rounds). Never sent to client. |
| Phone | phoneNumber | Optional |
| Points | number (0 decimals) | Running loyalty balance. Earned on order completion, debited on gift-card redemption. |
| TotalEarnedPoints | number (0) | Lifetime earned points. Drives tier promotion via `getTierForPoints()`. |
| TierLevel | singleSelect | `Rookie` / `Veteran` / `All-Star` / `Hall of Famer` |
| FavoriteSport | singleSelect | `Baseball` / `Basketball` / `Football` / `Hockey` / `Soccer` / `Multi-Sport` |
| FavoriteTeam | singleLineText | Free text (e.g., "Detroit Tigers") |
| HowDidYouHear | singleSelect | `Walked in / drove by` / `Friend or family` / `Social media` / `Online search` / `Card show` / `Other` |
| MarketingEmails | checkbox | Newsletter opt-in |
| MemberSince | date (ISO) | Set on signup |
| IsAdmin | checkbox | Gates admin functions + admin.html visibility |

**Functions reading/writing**: `signup.js` (create), `login.js` (lookup), `get-user.js`, `update-profile.js`, `change-password.js`, `admin-*` (admin check via `requireAdmin`).

---

## 2. Products (`tblT6Ps8MZn2utbgd`)

The 10–15 curated online-exclusive cards. The brick-and-mortar shop's "thousands" of cards are NOT in this table.

| Field | Type | Notes |
|---|---|---|
| Name | singleLineText | Product title (e.g., "1989 Upper Deck Ken Griffey Jr. Rookie") |
| Slug | singleLineText | URL-safe identifier |
| Category | singleSelect | `Vintage` / `Modern` / `Rookie` / `Memorabilia` / `Sealed Box` / `Graded` / `Multi-Sport` |
| Sport | singleSelect | `Baseball` / `Basketball` / `Football` / `Hockey` / `Soccer` / `Multi-Sport` |
| Price | currency (USD) | |
| BonusPoints | number (0) | Extra points awarded on top of base 1pt/$1 (for promo drops) |
| Description | multilineText | Card details, condition, grading notes |
| Year | number (0) | Card year (e.g., 1989) |
| Manufacturer | singleLineText | Topps / Upper Deck / Panini / etc. |
| Stock | number (0) | Hard count. 0 = sold out. |
| ImageURL | url | Cloudinary or hosted image |
| IsAvailable | checkbox | Master toggle to hide product without deleting |
| IsOnlineExclusive | checkbox | All Products SHOULD be true (defining feature) |
| SortOrder | number (0) | Display order in shop grid |

**Functions reading/writing**: `get-products.js` (public), `admin-products.js` (CRUD).

---

## 3. Orders (`tblOrdersTcXxxxx`)

Each customer order. References Users by record ID.

| Field | Type | Notes |
|---|---|---|
| OrderNumber | singleLineText | Auto-generated by `genOrderNumber()` (e.g., `TC-20260512-A1B2`) |
| UserID | multipleRecordLinks → Users | Order owner |
| UserEmail | email | Denormalized for admin filtering |
| Items | multilineText | JSON array: `[{productId, name, price, qty}, ...]` |
| ItemsList | multilineText | Human-readable summary for admin |
| Subtotal | currency | Before tax/discount |
| TaxAmount | currency | 6% Michigan sales tax |
| PointsRedeemed | number (0) | Points spent toward this order (100 = $1) |
| DiscountAmount | currency | Dollar value of redeemed points |
| TotalAmount | currency | Final charge after tax + discount |
| PointsEarned | number (0) | Awarded on order completion (base × tier multiplier + bonus) |
| Fulfillment | singleSelect | `Pickup` / `Shipping` |
| ShippingAddress | multilineText | Filled only for Shipping orders |
| Status | singleSelect | `Pending` / `Processing` / `Ready for Pickup` / `Shipped` / `Completed` / `Cancelled` |
| OrderDate | dateTime (ISO) | Server-set on creation |
| Notes | multilineText | Customer notes |

**Functions**: `create-order.js`, `get-orders.js`, `admin-orders.js`, `admin-update-order.js`.

**Points lifecycle**: Earned on Status → `Completed`. If admin cancels a completed order, points should be manually reversed (no auto-reverse function in v1).

---

## 4. Ratings (`tblRatingsTcXxxxx`)

5-star reviews on products. One row per (User, Product) pair (upsert).

| Field | Type | Notes |
|---|---|---|
| UserID | multipleRecordLinks → Users | |
| UserName | singleLineText | Denormalized for display |
| ProductID | multipleRecordLinks → Products | |
| Stars | number (0) | 1–5 |
| Comment | multilineText | Optional |
| CreatedAt | dateTime (ISO) | |
| UpdatedAt | dateTime (ISO) | Updated on re-rate |

**Functions**: `submit-rating.js` (upsert), `get-ratings.js`, `get-product-ratings.js`.

---

## 5. RewardsTiers (`tblTiersTcXxxxxx`)

Tier ladder. Seed with 4 rows:

| TierName | MinimumPoints | PointsMultiplier | PerksDescription |
|---|---|---|---|
| Rookie | 0 | 1.0 | Free to join. 1 point per dollar on every online order. |
| Veteran | 250 | 1.25 | Bigger pulls earn faster. Early access to mystery drops. |
| All-Star | 750 | 1.5 | Birthday bonus pack. First dibs on limited repacks. |
| Hall of Famer | 1500 | 2.0 | Top shelf. Private box breaks and shop-night invites. |

| Field | Type | Notes |
|---|---|---|
| TierName | singleLineText | Primary field |
| MinimumPoints | number (0) | Lifetime earned threshold |
| PointsMultiplier | number (2 decimals) | Multiplies base 1pt/$1 |
| PerksDescription | multilineText | Displayed on rewards.html |
| BadgeColor | singleLineText | CSS gradient or hex (optional — fallback to theme tier classes) |
| SortOrder | number (0) | 1–4 |

**Functions**: `get-tiers.js` (cached in `_shared.js getTierForPoints()`).

---

## 6. GiftCardRedemptions (`tblGiftCardsTcXxxxx`)

Points-to-gift-card conversions. 100 pts = $1, min 500 pts ($5) per redemption in 100-pt increments.

| Field | Type | Notes |
|---|---|---|
| Code | singleLineText | Auto-generated by `genGiftCardCode()` (e.g., `TC-GC-XXXX-XXXX`) |
| UserID | multipleRecordLinks → Users | |
| UserEmail | email | Denormalized |
| PointsSpent | number (0) | |
| DollarValue | currency | PointsSpent / 100 |
| Status | singleSelect | `Pending` / `Issued` / `Used` / `Cancelled` |
| RequestedAt | dateTime (ISO) | |
| IssuedAt | dateTime (ISO) | When admin issues code |
| UsedAt | dateTime (ISO) | When customer redeems in-store |
| Notes | multilineText | Admin notes |

**Functions**: `redeem-points.js` (customer creates Pending), `get-gift-cards.js`, `admin-update-giftcard.js`.

**Flow**: Customer redeems on rewards.html → row created with `Pending` status, points immediately debited from User. Admin reviews and switches to `Issued` (which delivers/displays the code). Cashier marks `Used` when redeemed in-store.

---

## 7. SiteImages (`tblImagesTcXxxxxxx`)

Slot-based image management. Frontend uses `data-image-slot="hero_main"` etc. to bind.

| Field | Type | Notes |
|---|---|---|
| SlotName | singleLineText | Primary key. Examples: `hero_main`, `about_banner`, `shop_banner` |
| ImageURL | url | Cloudinary or hosted URL |
| AltText | singleLineText | Accessibility |
| PageUsedOn | singleSelect | `Home` / `Shop` / `About` / `Rewards` / `Contact` |
| IsActive | checkbox | Allows scheduled swaps |

**Functions**: `get-site-config.js` (returns images alongside config).

**Required slots for v1**: `hero_main`, `about_banner`.

---

## 8. SiteConfig (`tblConfigTcXxxxxxx`)

Key/value content config. Frontend uses `data-config="hero_title"` etc. to bind.

| Field | Type | Notes |
|---|---|---|
| Key | singleLineText | Primary key |
| Value | multilineText | Text content |
| Description | singleLineText | What this controls (for admin clarity) |
| Category | singleSelect | `Homepage` / `About` / `Contact` / `Footer` / `Socials` / `SEO` |

**Required keys for v1**:

| Key | Example Value | Where Used |
|---|---|---|
| site_name | TC Sports Cards | All pages (header logo, title) |
| hero_title | Cardboard Heroes Live Here | index.html hero |
| hero_subtitle | From wax-pack classics to today's rookie hits — curated drops only available online. | index.html hero |
| about_title | A Hobby Shop Built by Collectors | index.html, about.html |
| about_body | TC Sports Cards is Zeeland's home base for sports card collectors of every era… | index.html, about.html |
| address_line1 | 123 Main Avenue | index.html, contact.html, footer |
| address_line2 | Zeeland, MI 49464 | index.html, contact.html, footer |
| phone | (616) 555-0100 | index.html, contact.html, footer |
| contact_email | hello@tcsportscards.com | index.html, contact.html, footer |
| hours_weekday | Mon–Fri: 11am – 7pm | index.html, contact.html |
| hours_saturday | Saturday: 10am – 6pm | index.html, contact.html |
| hours_sunday | Sunday: Closed | index.html, contact.html |
| footer_tag | Zeeland's hobby shop for vintage and modern sports cards. | footer all pages |

**Functions**: `get-site-config.js`.

---

## 9. BotConversations (`tblBotConvsTcXxxx`)

Append-only log of every turn between a site visitor and the Counter Helper bot. Powered by `netlify/functions/helper-bot.js` (OpenAI `gpt-3.5-turbo`). Per CLAUDE.md §28 (Universal Helper Bot).

| Field | Type | Notes |
|---|---|---|
| SessionID | singleLineText | Random per-tab ID (e.g., `tc-a1b2c3d4-l9k8m`). Groups all turns from one session. |
| UserID | singleLineText | User's email if logged in; empty string otherwise. (Field name kept generic for consistency across sites.) |
| UserMessage | multilineText | Latest user turn (clipped at 1000 chars before send) |
| AssistantMessage | multilineText | Bot reply (clipped at 2000 chars before write) |
| Model | singleLineText | e.g., `gpt-3.5-turbo` |
| TokensIn | number (0) | OpenAI `prompt_tokens` count |
| TokensOut | number (0) | OpenAI `completion_tokens` count |
| Timestamp | dateTime (ISO) | Server-set on each turn |
| FlaggedForReview | checkbox | Manual review marker — Aaron sets this on turns that need attention (bad refusal, hallucination, off-tone) |

**Functions**: `helper-bot.js` (fire-and-forget create per turn — never blocks the chat response).

**Cost guardrails** (mirrored from CLAUDE.md §28G):
- Model: `gpt-3.5-turbo` (cheapest mainstream chat model)
- `max_tokens`: 500 per response
- `temperature`: 0.5
- Last 12 turns sent to OpenAI per request (older trimmed client-side too at 24)
- Rate limit: 1 message / 4 seconds, 40 messages / session (client-side)
- Site context (products, tiers, SiteConfig) cached server-side for 5 minutes per cold start

**Review workflow**: Filter view in Airtable by `FlaggedForReview = true` for periodic audit. Use TokensIn + TokensOut to monitor cost per session.

---

## Cross-table conventions

- **Email is the identity key.** Always match case-insensitively in functions (`findUserByEmail` lowercases the lookup).
- **Date/timestamps** use ISO 8601 strings (e.g., `2026-05-12T14:30:00.000Z`). Airtable dateTime fields handle parsing.
- **Currency fields** display in USD with 2-decimal precision.
- **Admin role** lives entirely on `Users.IsAdmin`. Admin functions verify via `requireAdmin()` which decodes the JWT and re-checks the Users row.
- **Stock decrement** happens in `create-order.js` after order insert. Race conditions are theoretical at this scale; for v2 consider Airtable's atomic update pattern.

---

## Seed data checklist (post-base creation)

Before going live:

1. **RewardsTiers**: Insert all 4 tier rows (see §5 table above).
2. **Users**: Create at least one user with `IsAdmin = true` for admin.html access. Set TierLevel manually to `Hall of Famer` so admin sees the full tier UI.
3. **Products**: Add 10–15 online-exclusive items with photos, prices, stock counts.
4. **SiteConfig**: Populate all required keys in §8 table.
5. **SiteImages**: Upload hero and about banner images to Cloudinary; insert rows with matching SlotName.
6. **BotConversations**: Create the table (no seed rows needed — the helper-bot function will create rows on first chat). Add a view named `Flagged` filtered by `FlaggedForReview = true`.

---

## Required Airtable Views (recommended)

For each table, create at minimum:

- **Users**: "All Users" (default), "Admins" (filter IsAdmin=true), "By Tier" (group by TierLevel)
- **Products**: "Active" (IsAvailable=true), "Out of Stock" (Stock=0), "By Sport" (group)
- **Orders**: "Pending" (Status=Pending), "Today" (OrderDate is today), "By Status" (group)
- **GiftCardRedemptions**: "Pending Issue" (Status=Pending), "Active Codes" (Status=Issued)
- **Ratings**: "Recent" (sort by CreatedAt desc)
- **BotConversations**: "Recent" (sort by Timestamp desc), "Flagged" (filter FlaggedForReview=true)

---

## Cross-site credits note

TC Sports Cards is **not currently part** of the PVL / Globally Ballin shared credits ecosystem (`appo7PS69rWmzEPnC`). All points + gift cards live in this base only. If TC Sports Cards is later folded into the broader credits ledger, add `CREDITS_BASE_ID` to env vars and migrate `Points` writes to the canonical `Credits` + `CreditBalances` tables per CLAUDE.md §26B.
